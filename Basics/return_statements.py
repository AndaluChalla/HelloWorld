def square(number):
   # return number * number
    print(number * number)

result = square(3)
print(result)
print(square(3)) ### we can also write code like this

#### if we don't add return statement function returns NONE by default
