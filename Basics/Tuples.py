numbers = (1,2,3) ##### Lists and tuples are mostly the same have square brackets and tuples have paranthesis and we can't change tuple values they are immutable
numbers[0] =10 ##### this will throw an error as we can't change values in tuples\
print(numbers[0])
